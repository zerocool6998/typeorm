import {ConnectionOptions} from "../connection/ConnectionOptions";

/**
 * DataSourceOptions is an interface with settings and options for specific DataSource.
 * Options contain database connection settings and other data-source related settings.
 */
export type DataSourceOptions = ConnectionOptions
